# Приложению не нужны сторонние пакеты — используется только
# стандартная библиотека Python (tkinter, sqlite3, hashlib).
#
# В Linux, если модуль tkinter отсутствует, установите системный пакет:
#   sudo apt install python3-tk
